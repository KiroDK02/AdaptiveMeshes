<UserControl x:Class="Views.UserControls.ProblemUserControls.SolutionPointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Views.UserControls"
             xmlns:vm="clr-namespace:ViewModels;assembly=ViewModels"
             xmlns:vmProblem="clr-namespace:ViewModels.ProblemViewModels;assembly=ViewModels"
             xmlns:converters="clr-namespace:Views.Converters"
             d:DataContext="{d:DesignInstance Type=vmProblem:SolutionPointsViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>

    <Expander Header="Значения в точках">
        <StackPanel Margin="10, 5, 0, 0">
            <ItemsControl ItemsSource="{Binding Points}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:PointViewModel}">
                        <Grid Margin="0, 2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="X:" VerticalAlignment="Center" Margin="0,0,3,0"/>
                            <TextBox Grid.Column="1" Text="{Binding X, Mode=TwoWay}"/>
                            
                            <TextBlock Grid.Column="2" Text="Y:" VerticalAlignment="Center" Margin="5,0,3,0"/>
                            <TextBox Grid.Column="3" Text="{Binding Y, Mode=TwoWay}"/>
                            
                            <TextBlock Grid.Column="4" Text="=" VerticalAlignment="Center" Margin="5,0,3,0"
                                       Visibility="{Binding Value, 
                                            Converter={StaticResource NullToVisibilityConverter}}"/>
                            
                            <TextBlock Grid.Column="5"
                                       Text="{Binding Value, StringFormat={}{0:E4}}"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding Value,
                                            Converter={StaticResource NullToVisibilityConverter}}"/>
                            
                            <Button Grid.Column="6" Content="✕" Margin="5,0,0,0"
                                    Command="{Binding DataContext.RemovePointCommand,
                                        RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <Button Content="Добавить точку"
                        Command="{Binding AddPointCommand}"
                        Margin="0,0,5,0"/>
                
                <Button Content="Рассчитать"
                        Command="{Binding CalculateCommand}"
                        Margin="0,0,5,0"/>
                
                <Button Content="Очистить"
                        Command="{Binding ClearPointsCommand}"
                        Margin="0,0,5,0"/>
            </StackPanel>
        </StackPanel>
    </Expander>

</UserControl>