<UserControl x:Class="Views.UserControls.AdaptationUserControls.AdaptationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Views.UserControls"
             xmlns:vm="clr-namespace:ViewModels.ProblemViewModels;assembly=ViewModels"
             xmlns:adaptationVms="clr-namespace:ViewModels.AdaptationViewModels;assembly=ViewModels"
             xmlns:adaptationViews="clr-namespace:Views.UserControls.AdaptationUserControls"
             xmlns:converters="clr-namespace:Views.Converters"
             d:DataContext="{d:DesignInstance Type=adaptationVms:AdaptationViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
    </UserControl.Resources>

    <Expander Header="Адаптация"
              IsExpanded="False">
        <StackPanel Margin="10,5,0,5">

            <TextBlock Text="Стратегия вычисления ошибки"
                       Margin="0,0,0,3" />
           
            <ComboBox ItemsSource="{Binding 
            Source={x:Static adaptationVms:CalculationErrorStrategyTypeHelper.Values}}"
                      SelectedItem="{Binding CalculationErrorStrategy}"
                      Margin="0,0,0,8">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding 
                                        Converter={StaticResource EnumDescriptionConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <TextBlock Text="Тип адаптации"
                       Margin="0,0,0,3"/>
            
            <ComboBox ItemsSource="{Binding 
                                        Source={x:Static adaptationVms:AdaptationTypeHelper.Values}}"
                      SelectedItem="{Binding AdaptationType}"
                      Margin="0,0,0,8"/>
            
            <adaptationViews:DistributionScalesView 
                DataContext="{Binding DistributionScalesViewModel}"/>
            
        </StackPanel>
    </Expander>
</UserControl>