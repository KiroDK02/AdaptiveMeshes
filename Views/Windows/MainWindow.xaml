<Window x:Class="Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModels;assembly=ViewModels"
        xmlns:scottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:local="clr-namespace:Views.Windows"
        xmlns:controls="clr-namespace:Views.UserControls"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Kiro2D" 
        Height="1200" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        
        <scottPlot:WpfPlot Grid.Row="1" Grid.Column="0"
                           Grid.RowSpan="2"
                           DataContext="{Binding MeshPlotViewModel}"
                           x:Name="MainMeshPlot"/>
        
        <controls:ProblemsEditorPanel Grid.Row="1" Grid.Column="1"
                                      Grid.RowSpan="2"
                                      DataContext="{Binding ProblemsEditorViewModel}" />
    </Grid>
</Window>
